# Image-Captioning-using-AI
This project demonstrates an AI-based Image Captioning system that automatically generates meaningful textual descriptions for input images. The model uses a pre-trained deep learning architecture.  🚀 Features  Automatic image-to-text caption generation Uses pre-trained deep learning models Fast and efficient (no heavy training required) 
